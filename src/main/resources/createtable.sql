CREATE TABLE `testdatabase`.`name` (
                                       `idname` INT NOT NULL,
                                       `lastname` VARCHAR(45) NULL,
                                       `firtname` VARCHAR(45) NULL,
                                       PRIMARY KEY (`idname`));
